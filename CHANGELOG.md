# 更新日志

本文件记录 `astrbot_plugin_helpmenu` 的主要变更。

## v1.1.0 - 2026-09-02

本次以**出图速度**为主题：通过"后台预渲染 + 本地预取池"把用户端等待时间降到 0，同时补上并发保护和冷却限制。

### 新增

- 新增菜单预渲染池：后台提前把整张菜单图渲染好，触发时直接秒发、用完即删、随后自动补仓，背景依然每次随机。由 `menu_pool_size` 配置（默认 1，设为 0 关闭）。
- 新增背景图预取池：提前从背景 API 拉图缓存到本地，渲染时随机取用、用完即删，既保留随机性又不必等待网络。由 `bg_pool_size` 配置（默认 2）。
- 新增插件加载完成后的预热：框架启动时即开始预渲染，首次触发帮助菜单也能秒发。
- 新增用户级触发冷却，由 `cooldown_seconds` 配置（默认 60 秒，0 表示不限制），冷却期内再次触发会提示剩余秒数。
- 新增并发防重：现场渲染期间重复触发会提示"帮助菜单正在生成中"，不会叠加渲染任务。
- 新增渲染串行锁，避免多个渲染任务同时抢占 CPU。
- 新增 `image_format` 配置项（`jpeg` / `png`，**默认 `jpeg`**）与 `image_quality` 配置项（1-100，默认 90，仅 jpeg 生效）。
- 新增成品图缓存：背景确定（未启用 API 背景且指定了单个本地背景文件）时缓存整张菜单图，后续命中直接秒发。

### 变更

- **菜单图片默认输出格式由 png 改为 jpeg。** 同一张长菜单实测编码耗时 3137ms → 1798ms（约 1.8 倍），体积 1.45MB → 0.84MB（约一半），肉眼几乎无差别。需要无损输出可在配置中改回 `png`。
- 菜单字体由 Base64 内联改为 `file://` 本地直链，Chromium 直接读取本地字体文件，省去数 MB Base64 的编码与解析开销。
- 页面等待策略由 `networkidle` + 固定 `wait_for_timeout` 改为 `load` + 显式等待 `document.fonts.ready` 和图片解码完成。页面资源均为本地或 data URL，`networkidle` 只会白等固定超时。
- 移除 HTML 模板中对 Google Fonts（`Noto Color Emoji`）的远程 `@import`，改用系统已安装的同名字体，消除每次渲染的网络阻塞。
- 背景图获取由"渲染时同步请求 API"改为"消费本地预取池 + 后台异步补仓"，渲染路径上不再有网络等待。
- Playwright 浏览器实例改为跨请求复用，不再每次渲染重启浏览器。

### 修复

- **修复菜单图下半部分没有背景、只剩纯色底的问题。** 背景层原为 `position: fixed`，只跟随视口高度；菜单较长时截图高度远超视口，导致下半部分背景缺失。现改为 `position: absolute` 并按文档实际高度铺满。
- 修复切换图片格式或质量后，缓存与预渲染池中仍残留旧格式图片的问题：输出格式与质量已并入缓存 key，变更后自动失效。
- 修复插件进程重启后，上一次运行残留的池图与成品缓存图不会被清理的问题。
- 修复菜单内容在后台补仓期间被修改时，过期图片仍可能进入预渲染池的问题。
- 修复池图在消息发送完成前就被删除，可能导致发图失败的时序竞争：已发送的池图改为延后清理。
- 修复背景预取池中的图片在读取前就被删除，导致背景编码失败的问题。

### 配置项变更

| 配置项 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- |
| `menu_pool_size` | int | `1` | 菜单预渲染池大小，0 表示关闭预渲染 |
| `bg_pool_size` | int | `2` | 背景图预取池大小 |
| `cooldown_seconds` | int | `60` | 同一用户两次触发的最小间隔，0 表示不限制 |
| `image_format` | string | `jpeg` | 菜单图片格式，可选 `jpeg` / `png` |
| `image_quality` | int | `90` | JPEG 质量 1-100，仅 `image_format` 为 `jpeg` 时生效 |

### 升级须知

- 本次更新会改变菜单图片的默认输出格式（png → jpeg）。若对图片有无损要求，请在插件配置中把「菜单图片格式」改回 `png`。
- 缓存目录 `data/plugin_data/astrbot_plugin_helpmenu/cache/` 会新增 `bg_pool/` 子目录用于存放预取的背景图，属正常缓存，会自动补仓和清理。
- 预渲染会在插件加载后立即在后台占用一次 CPU 渲染菜单图。若不希望有后台开销，可将 `menu_pool_size` 设为 0，恢复为每次现场生成。
- 更新后需要在 AstrBot WebUI 中重载插件才会生效。

## v1.0.0 - 2026-06-10

### 新增

- 新增图片版帮助菜单，支持通过配置的触发命令查看完整菜单。
- 新增分类筛选能力，支持在触发命令后追加分类名查看指定分类。
- 新增 Native Page 后台管理页面，可视化维护分类、指令、描述和 Logo。
- 新增分类 Logo、指令 Logo 与标题圆形 Logo 上传能力。
- 新增指令图标 emoji 自动回退规则，未配置 Logo 时根据指令名和描述自动匹配图标。
- 新增 `command_aliases` 配置项，支持为帮助菜单配置多个触发别名。
- 新增 `font_file` 配置项，支持选择内置字体或回退系统字体。

### 变更

- 插件版本重新整理为 `1.0.0`，按 AstrBot 官方插件格式规范化发布文件。
- 插件元数据改为标准 `metadata.yaml` 字段，支持平台仅声明 `aiocqhttp`。
- `README.md` 重写为面向插件市场发布的安装、使用、配置和数据说明文档。
- `menu.json` 改为持久化数据文件，不再作为插件目录中的可变文件维护。
- 插件目录中的默认菜单模板改名为 `default_menu.json`，仅用于首次初始化。
- HTML 渲染器读取相对资源时，优先从插件持久化数据目录查找，再回退到插件内置资源。
- 帮助菜单触发逻辑改为运行时按配置匹配主命令和别名，避免静态 `@filter.command("help")` 导致配置不生效。
- 标题旁边的圆形 Logo 改为通过 Native Page 上传和清空，不再通过配置项手写相对路径。

### 修复

- 修复 `command_name` 配置只被读取但不会影响实际触发命令的问题。
- 修复指令别名配置不生效的问题。
- 修复后台上传图标和菜单数据写入插件目录，更新或重装插件后可能丢失的问题。
- 修复默认 Logo 配置仍指向 `./logo.jpg`，但插件实际提供 `logo.png` 的问题。
- 移除 `logo_path` 配置项，避免用户在配置页手写插件目录相对路径。

### 数据迁移

- 菜单数据保存到 AstrBot 持久化目录：

  ```text
  data/plugin_data/astrbot_plugin_helpmenu/menu.json
  ```

- 后台上传的图标保存到：

  ```text
  data/plugin_data/astrbot_plugin_helpmenu/images/icons/
  ```

- 首次启动时，如果持久化目录中不存在菜单数据，会自动从插件目录中的 `default_menu.json` 初始化。
- 旧版本若仍在插件目录中存在 `menu.json` 或 `images/icons/`，插件加载时会尝试自动迁移到持久化目录。

### 依赖

- `playwright>=1.40.0`
- `jinja2>=3.0.0`
- `aiohttp>=3.8.0`

### 升级须知

- 修改 `command_name` 或 `command_aliases` 后，需要在 AstrBot WebUI 中重载插件后生效。
- 更新或重装插件前，请保留 `data/plugin_data/astrbot_plugin_helpmenu/` 目录，以免丢失自定义菜单和上传图标。
