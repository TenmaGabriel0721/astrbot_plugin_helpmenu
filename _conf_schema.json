{
  "command_prefix": {
    "description": "指令前缀",
    "type": "string",
    "hint": "显示在菜单中的指令前缀，如 ~ 或 / 或 ！，留空则自动获取系统前缀",
    "default": ""
  },
  "command_name": {
    "description": "触发命令",
    "type": "string",
    "hint": "帮助菜单主触发命令，如 help 或 菜单。修改后请重载插件生效。",
    "default": "help"
  },
  "command_aliases": {
    "description": "触发命令别名",
    "type": "string",
    "hint": "额外触发别名，支持逗号、中文逗号、分号、竖线或换行分隔，如 菜单,帮助。修改后请重载插件生效。",
    "default": "菜单,帮助"
  },
  "header_title": {
    "description": "菜单标题",
    "type": "string",
    "default": "月月菜单"
  },
  "header_subtitle": {
    "description": "副标题",
    "type": "string",
    "default": "System Menu"
  },
  "footer_text": {
    "description": "页脚文字",
    "type": "string",
    "default": "[参数]必选 <参数>可选 | 请注意参数之间的空格"
  },
  "theme_color": {
    "description": "主题色",
    "type": "string",
    "default": "#667eea",
    "hint": "十六进制颜色代码，如 #667eea（蓝紫色）、#FF88AA（粉色）"
  },
  "use_api_background": {
    "description": "使用API获取背景图",
    "type": "bool",
    "hint": "开启后后台自动从API预取背景图缓存到本地，触发时直接使用（随机取用、用完即删、后台自动补仓），全程零网络等待；关闭则使用本地图片",
    "default": true
  },
  "background_api": {
    "description": "背景图API地址",
    "type": "string",
    "hint": "返回图片的API地址，留空使用默认API",
    "default": "http://manyacg.top/setu"
  },
  "menu_pool_size": {
    "description": "菜单预渲染池大小",
    "type": "int",
    "hint": "后台提前渲染好的完整菜单图数量(0-3)。触发时直接秒发、用完即删、后台自动补仓，背景依然每次随机。0=关闭预渲染（每次现场生成，需等待数秒）。",
    "default": 1
  },
  "bg_pool_size": {
    "description": "背景预取池大小",
    "type": "int",
    "hint": "后台预缓存的API背景图数量(1-5)。每用掉一张即随机换新，后台自动补足。无需等待网络。",
    "default": 2
  },
  "cooldown_seconds": {
    "description": "用户触发冷却(秒)",
    "type": "int",
    "hint": "同一用户两次触发帮助菜单的最小间隔(0-3600秒)，0表示不限制。冷却期内再次触发会提示剩余秒数。",
    "default": 60
  },
  "image_format": {
    "description": "菜单图片格式",
    "type": "string",
    "hint": "jpeg 编码速度约为 png 的 1.8 倍、体积约为一半（长菜单实测 3.1s/1.45MB → 1.8s/0.84MB），肉眼几乎无差别，默认推荐。png 为无损，纯色/文字边缘更锐利但更慢、更占空间。",
    "options": ["jpeg", "png"],
    "default": "jpeg"
  },
  "image_quality": {
    "description": "JPEG 质量",
    "type": "int",
    "hint": "1-100，仅在图片格式为 jpeg 时生效。90 为清晰度与体积的平衡点，低于 80 会出现可见压缩痕迹。",
    "default": 90
  },
  "background_image": {
    "description": "本地背景图片路径",
    "type": "string",
    "hint": "当不使用API时，可以是单个图片或包含多张图片的文件夹（随机选择）",
    "default": "./images"
  },
  "blur_radius": {
    "description": "背景虚化程度",
    "type": "int",
    "hint": "背景图片的模糊程度(0-50)，0表示不模糊",
    "default": 0
  },
  "card_opacity": {
    "description": "卡片透明度",
    "type": "int",
    "hint": "命令卡片的不透明度(0-100)。数值越低越透明",
    "default": 10
  },
  "font_file": {
    "description": "菜单字体",
    "type": "string",
    "hint": "选择渲染菜单使用的字体。auto=自动选用 fonts/std/ 中的默认字体；none=不嵌入字体，回退到系统字体；其它选项对应 fonts/std/ 下的字体文件。若要添加新字体，把字体文件放入 fonts/std/ 目录，并在此处填写文件名（如 myfont.ttf）。",
    "options": [
      "auto",
      "none",
      "东京街角的小浪漫.ttf",
      "font.ttf"
    ],
    "default": "auto"
  }
}